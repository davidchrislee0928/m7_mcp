# =====================================================================
# 🛡️ M7-ALPHA 量化主权控制仓 · 远程仓库铁血风控隔离大坝
# =====================================================================

# ---------------------------------------------------------------------
# 1. Node.js 核心熔断区（严禁巨型本地依赖污染远程）
# ---------------------------------------------------------------------
node_modules/
/npm-debug.log*
/yarn-error.log*
/pnpm-debug.log*

# ---------------------------------------------------------------------
# 2. Python 运行期缓存与二进制编译挂件
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------
# 3. 现代 Python 包管理器虚拟环境（uv / poetry / venv）
# ---------------------------------------------------------------------
.venv/
venv/
ENV/
env/
.pdm-plugins/
.pdm-build/

# 🔴 特别提示：
# requirements.txt, pyproject.toml, uv.lock, package-lock.json 
# 以上 4 个属于“环境还原高精地图”，千万不要加进这里，必须正常推送到 GitHub！

# ---------------------------------------------------------------------
# 4. 铁血风控层：生产环境核心密钥物理隔离
# ---------------------------------------------------------------------
.env
.env.local
.env.*.local
*.env
# 严禁将任何带有 API_KEY 的本地配置文件推向公网

# ---------------------------------------------------------------------
# 5. 物理资产层：FMP原始财报与高频价格 Parquet 缓存盾牌
# ---------------------------------------------------------------------
# 机制说明：本地运行产生的冷热财报及 10 年价格长卷无需推上远程，
# 线上虚拟机部署点火后，会通过其自愈数据打捞网关重新拉取。
#data_cache/
#fmp_cache_*.json
#*.parquet

# ---------------------------------------------------------------------
# 6. 操作系统与本地 IDE 脏挂件
# ---------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 编辑器配置
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp